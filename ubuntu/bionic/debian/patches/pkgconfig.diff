Index: node-16.15.1/node.pc
===================================================================
--- /dev/null
+++ node-16.15.1/node.pc
@@ -0,0 +1,10 @@
+prefix=/usr
+exec_prefix=/usr
+libdir=/usr/lib
+includedir=${prefix}/include/node
+
+Name: node
+Description: Embedded Node.js
+Version: _VERSION_
+Cflags: -I${includedir}/node
+Libs: -L${libdir} -lnode
